
- elenco dei problemi di compilazione incontrati: vedi checklst.txt nella directory prjfold

directory Baselib
       libreria baselib
directory Cgi
       programma chiamato da I-VIEW che interfaccia il daemon per
       ottenere le soluzioni. L'interfacciamento avviene mediante
       passaggio di parametri sulla linea di comando
directory Daemon
       programma daemon. La direct. Fasatura valida è quella sotto
       dir. Motore
directory hpfs
       contiene le pagine html usate da I-VIEW/cgi
directory integraz\subsys\motore\prepdati
       contiene i sorgenti per il caricamento
directory motore
       libreria motore
directory motore2
       libreria motore2 per il caricamento
directory porting
       sorgenti utilizzati in baselib e necessari per il porting verso Windows
directory prjfold
       informazioni sul progetto
directory procomp
       sorgenti per il test di scelta del compilatore
directory stazconv e stazconv.win
       per la conversione verso formato Windows del file id_stazi.db
directory testbit
       per il test dei bitfields
